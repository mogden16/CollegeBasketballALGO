{
  "name": "college-basketball-cloudflare-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "refresh:data": "python3 scripts/regenerate_team_models.py && python3 scripts/regenerate_games_by_date.py && python3 scripts/generate_picks.py",
    "refresh:models": "python3 scripts/regenerate_team_models.py",
    "check:deploy": "bash scripts/predeploy_check.sh",
    "deploy:safe": "npm run check:deploy && npm run deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260310.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.6.0"
  }
}
